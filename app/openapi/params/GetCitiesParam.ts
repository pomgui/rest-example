/**
 * Parameters for operation 'getCities' (GET /cities)
 * List of cities being monitored by the server
 */

/** Parameters sent in the query */
export interface GetCitiesQueryParam {
    
    dummy?: string;
}

/** Structure with ALL the operation parameters */
export interface GetCitiesParam extends GetCitiesQueryParam {
}